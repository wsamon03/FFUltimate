requests>=2.31.0
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
tqdm>=4.66.0  # Progress bar for batch processing
asyncpg>=0.29.0  # High-performance async Postgres driver
fastapi[standard]>=0.100.0  # For background tasks
celery[redis]>=5.3.0  # For async task queue (alternative to background tasks)
redis>=5.0.0

# user_api dependencies
authlib>=1.3.0              # OAuth 2.0 PKCE client (Google + Azure AD)
python-jose[cryptography]>=3.3.0  # JWT issue/verify (HS256)
httpx>=0.27.0               # Async HTTP client for OAuth token exchange
itsdangerous>=2.2.0         # Signed state parameter for CSRF protection in OAuth flow
